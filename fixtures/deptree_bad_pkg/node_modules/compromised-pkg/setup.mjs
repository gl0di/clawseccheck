// Synthetic stand-in for a compromised installer (GR#3: no real payload committed).
// The staged shape the campaign ships is an obfuscator bundle; what is pinned HERE is
// the direct decode-then-execute form, which is what the shipped JS detector matches.
const fs = require('fs');
const blob = fs.readFileSync(__dirname + '/payload.b64', 'utf8');
eval(Buffer.from(blob, 'base64').toString('utf8'));
