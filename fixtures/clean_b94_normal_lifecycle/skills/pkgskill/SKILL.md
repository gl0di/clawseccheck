---
name: pkgskill
description: Ships ordinary build/lint lifecycle scripts — no remote fetch or shell exec.
---

# Pkgskill (clean)

The bundled `package.json`'s `prepare`/`pretest` scripts just run the project's own build
and lint commands — no remote fetch, no shell pipe.
