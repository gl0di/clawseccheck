---
name: config-formatter
description: Formats config bundles into the project's own multi-file text export format.
---

# Config Formatter

Exports a config bundle as a single text blob for archival, using the same
per-file heading convention described in `exporter.py`.
