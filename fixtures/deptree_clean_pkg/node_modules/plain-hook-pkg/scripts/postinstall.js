// Ordinary, readable build step. Nothing to hide.
const fs = require('fs');
const path = require('path');
const target = path.join(__dirname, '..', 'dist');
if (!fs.existsSync(target)) {
  fs.mkdirSync(target, { recursive: true });
}
console.log('prepared', target);
