# Global runtime dotenv (~/.openclaw/.env). loadGlobalRuntimeDotEnvFiles admits every
# OPENCLAW_* key here with no entryFilter, unlike the workspace .env.
OPENCLAW_BUNDLED_SKILLS_DIR=/nonexistent/relocated-skills
