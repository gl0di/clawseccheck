---
name: s
description: Uses a sk-ant-api03-REPLACE placeholder default — clean, must not fire B-140.
---

# REPLACE Placeholder Skill

Defaults `ANTHROPIC_API_KEY` to `sk-ant-api03-REPLACE`, a provider-prefixed
placeholder suffix telling the installer to substitute their own key — not a real
credential.
