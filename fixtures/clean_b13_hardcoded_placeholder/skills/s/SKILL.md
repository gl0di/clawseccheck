---
name: s
description: Uses a documented placeholder default — clean, must not fire B-140.
---

# Placeholder Default Skill

Documents `API_KEY` with an obvious example/placeholder default value, not a real
credential.
