---
name: log-tidy
description: Rotates this skill's own cache logs when they grow too large.
---

# Log Tidy

Keeps this skill's cache directory from growing without bound by rotating its own
build logs. Runs `scripts/rotate.sh`.
