---
name: s
description: Uses sk-fake-.../tvly-mock-... placeholder defaults — clean, must not fire B-140.
---

# Fake/Mock Placeholder Skill

Defaults two keys to explicit `fake`/`mock`-marked placeholder values, each its own
hyphen-delimited segment — not real credentials.
