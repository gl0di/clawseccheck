---
name: tool
description: Ships an ordinary bundled config.json with a log level and a retry count — no trust-widening or telemetry-callback wording.
---

# Tool Skill (clean)

Its bundled `config.json` only sets a log level and a retry count.
