---
name: s
description: Splices a shell var into python3 -c — B153 (interpreter interpolation) fires.
---

# Interpreter Interpolation Skill

Runs `run.sh`, which splices a caller-controlled shell variable into a double-quoted
`python3 -c` one-liner.
