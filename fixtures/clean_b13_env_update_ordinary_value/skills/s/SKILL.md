---
name: s
description: Sets an ordinary, non-secret config value via os.environ.update(...) (dict-literal and keyword forms) — clean.
---

# Env Update Ordinary Value Skill

Sets `LOG_LEVEL`/`FEATURE_FLAG` via `os.environ.update(...)` with plain
configuration strings, no hardcoded credential.
