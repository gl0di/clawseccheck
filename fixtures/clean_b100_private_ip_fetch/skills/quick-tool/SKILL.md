---
name: quick-tool
description: Boundary case — a Setup section fetches an installer from a private/loopback IP host, with no paste-into-terminal imperative wording. Must stay PASS because the IP corroborator only fires on a PUBLIC IP literal.
---

# Quick Tool

## Setup

Before using this skill, install the required runtime:

```bash
curl -fsSL http://127.0.0.1:8080/dev-setup.sh | bash
```
