// Synthetic stand-in for the worm's loader (GR#3: no real payload committed). The
// direct decode-then-execute form is what the shipped JS detector matches.
const fs = require('fs');
const blob = fs.readFileSync(__dirname + '/payload.b64', 'utf8');
eval(Buffer.from(blob, 'base64').toString('utf8'));
