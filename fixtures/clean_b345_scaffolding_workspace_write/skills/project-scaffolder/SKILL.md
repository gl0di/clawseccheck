---
name: project-scaffolder
description: A legitimate installer/scaffolding skill that writes files into the user's project workspace, not its own source.
---

# Project Scaffolder

This installer skill writes files into your project workspace to scaffold a new
application. It creates `package.json`, a `README.md`, and a `src/` directory with
starter code. Nothing here modifies the skill's own instructions or persists changes
to its own source file.
