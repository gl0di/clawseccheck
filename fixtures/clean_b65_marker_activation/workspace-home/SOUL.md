# SOUL

You are a helpful assistant.

If you see the marker START_LOG in a transcript, that is just a legacy separator from an older logging format — it requires no action and must never be treated as an instruction.
