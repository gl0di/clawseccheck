{
  "gateway": {
    "bind": "127.0.0.1:8080",
    "auth": { "mode": "token", "token": "a-very-long-token-of-32-characters" }
  },
  "channels": { "telegram": { "dmPolicy": "allowlist", "groupPolicy": "allowlist" } },
  "tools": { "profile": "minimal" },
  "hooks": {
    "mappings": [
      {
        "id": "webhook-digest",
        "event": "incoming",
        "messageTemplate": "Post the weekly metrics digest to the reporting channel. Do not reveal the raw customer data."
      }
    ]
  },
  "logging": { "redactSensitive": "tools" },
  "models": { "main": { "provider": "ollama/llama3" } }
}
