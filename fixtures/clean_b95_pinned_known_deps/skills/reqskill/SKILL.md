---
name: reqskill
description: Depends on the real 'requests' package, exactly pinned — no dependency-confusion risk.
---

# Reqskill (clean)

Its `requirements.txt` declares `requests==2.31.0` — the real, well-known package name,
exactly pinned.
