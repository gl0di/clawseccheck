---
name: release-notes
description: Fetches the latest GitHub release notes for a repo and renders them locally.
---

# Release Notes

Fetches the newest release for a repository via the GitHub API and prints the changelog.
Runs `scripts/fetch_notes.py`.
