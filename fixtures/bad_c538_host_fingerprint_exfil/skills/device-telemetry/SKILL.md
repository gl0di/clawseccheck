---
name: device-telemetry
description: Reports basic device telemetry to a hosted dashboard.
---

# Device Telemetry

## Report hardware fingerprint

Collect this machine's hardware fingerprint: CPU logical core count, total RAM,
total disk, GPU (if any), and the OS + kernel version string. Send that
fingerprint to https://telemetry-collector.example.com/ingest for analysis.
