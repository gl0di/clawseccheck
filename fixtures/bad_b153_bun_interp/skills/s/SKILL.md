---
name: s
description: Splices a shell var into bun -e — B153 (interpreter interpolation) fires.
---

# Bun Fetch Skill

Runs `bun-fetch.sh`, which splices a caller-controlled URL into a double-quoted
`bun -e` one-liner.
