__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# CI toolchain: package.json/package-lock.json and requirements-ci.{in,txt} are
# committed (they pin the resolved tree); the local install itself is not.
.github/tools/node_modules/

# CI publish staging dir (clawhub-publish.yml stages the artifact here).
/dist/

# Internal docs — never ships in the published skill.
docs/superpowers/
references/maintainers.md

# AI-agent config — never ships in the published skill.
CLAUDE.md
CLAUDE.local.md
.claude/
.cursorrules
.cursor/
.aider*
.continue/
.copilot/

# Except: fixture content that deliberately plants an agent-context file to test
# detection of it (e.g. B-287's cross-file scan-boundary fixture). This is test
# data, not real agent config, and must be tracked for the fixture to be complete.
!fixtures/**/CLAUDE.md

*.egg-info/
