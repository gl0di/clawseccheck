[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "clawcheck"
dynamic = ["version"]
description = "Local, read-only OpenClaw security self-audit (zero dependencies)."
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.9"
authors = [{ name = "gl0di" }]
keywords = ["openclaw", "security", "audit", "ai-agent", "prompt-injection", "clawhub"]
classifiers = [
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Topic :: Security",
    "Environment :: Console",
]
dependencies = []

[project.urls]
Homepage = "https://github.com/gl0di/clawcheck"
Issues = "https://github.com/gl0di/clawcheck/issues"

[project.scripts]
clawcheck = "clawcheck.cli:main"

[tool.setuptools]
packages = ["clawcheck"]

[tool.setuptools.dynamic]
version = { attr = "clawcheck.__version__" }
